const Commands = require('../../../models/Commands')
const Contests = require('../../../models/Contests')
const Duels = require('../../../models/Duels')
const Guilds = require('../../../models/Guilds')
const Items = require('../../../models/Items')
const Npc = require('../../../models/Npc')
const Pets = require('../../../models/Pets')
const Pokemon = require('../../../models/Pokemon')
const Refs = require('../../../models/Refs')
const Ribbons = require('../../../models/Ribbons')
const Spawn = require('../../../models/Spawn')
const Traders = require('../../../models/Traders')
const Trainers = require('../../../models/Trainers')

const { SyncInitials } = require('./default/Initials')
const { SyncRegions } = require('./default/Regions')

const Sync = () => {
    // Commands.sync()
    // Contests.sync()
    // Duels.sync()
    // Guilds.sync()
    // Items.sync()
    // Npc.sync()
    // Pets.sync()
    // Pokemon.sync()
    // Refs.sync()
    // Ribbons.sync()
    // Spawn.sync()
    // Traders.sync()
    // Trainers.sync()

    // SyncInitials()
    // SyncRegions()
}

module.exports = { Sync }